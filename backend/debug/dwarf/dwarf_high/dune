(include_subdirs unqualified)

(library
  (name dwarf_high)
  (wrapped true)
  (flags (:standard -principal -nostdlib -open Dwarf_deps))
  (ocamlopt_flags (:standard -O3))
  (libraries stdlib dwarf_low dwarf_deps)
)
